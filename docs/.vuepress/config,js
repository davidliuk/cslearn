const { defaultTheme } = require("vuepress")

module.export = {
  // base: "/cslearn/", //<仓库名>
  theme: defaultTheme({
    // // url
    // logo: 'https://vuejs.org/images/logo.png',
    // 默认主题配置
    navbar: [
      {
        text: "首页",
        link: "/",
      },
      {
        text: "UNIX",
        link: "/unix/",
      },
    ],
  }),
};
